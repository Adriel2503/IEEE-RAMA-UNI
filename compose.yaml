services:
  web:
    build:
      context: .
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
